export enum PolicyLanguage {
  EN = 'en',
  KO = 'ko'
}

export enum PolicyType {
  PRIVACY = 'privacy',
  TERMS = 'terms',
  WITHDRAW = 'withdraw'
}

export enum PortalType {
  PUBLIC = 'public',
  VOTE = 'vote',
  GOONG_HAP = 'goong-hap',
  PIC = 'pic',
  COMMUNITY = 'community',
  NOVEL = 'novel',
  MYPAGE = 'mypage',
  MEDIA = 'media',
  SHOP = 'shop',
  AUTH = 'auth',
  PRIVATE = 'private'
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female'
}
